{
  "name": "@rtc/mediasoup",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cross-env DEBUG='mediasoup:ERROR:Worker,mediasoup:Worker' tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@rtc/packages": "workspace:*",
    "dotenv": "^16.4.7",
    "mediasoup": "^3.0.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "cross-env": "^10.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
